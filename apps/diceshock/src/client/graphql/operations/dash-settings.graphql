query CaptchaSettings {
  captchaSettings {
    enabled
    disabledUntil
  }
}

mutation SetCaptchaEnabled($enabled: Boolean!) {
  setCaptchaEnabled(enabled: $enabled) {
    enabled
    disabledUntil
  }
}
